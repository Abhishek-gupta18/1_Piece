[project]
name = "vault"
version = "0.1.0"
description = "A fault-tolerant distributed object storage system"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["grpcio>=1.62", "grpcio-tools>=1.62", "fastapi>=0.109", "uvicorn>=0.27"]